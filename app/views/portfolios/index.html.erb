
    <h1>Moje instrumenty finansowe</h1>

    <table class="table table-striped">
        <thead>
            <tr>
			    <th>Nazwa</th>
			    <th>Nazwa przyjazna</th>
			    <th>Opis</th>
          <th>Ilość jednostek</th>
			    <th>Wartość</th>
			    <th>Data zakupu</th>

			    <th>Typ</th>
          <th></th>
          <th></th>
            </tr>
        </thead>
        <tbody>
          <% @portfolios.each do |portfolio|  %>
             <%#= render portfolio.instruments, locals: {portfolio: @portfolio} %>
             <%= render partial: "instruments/instrument", collection: portfolio.instruments, locals: {portfolio: portfolio} %>
          <% end %>
        </tbody>
    </table>
    <%= link_to 'Nowy instrument finansowy', new_portfolio_instrument_path(@portfolios.first) %>    
<br />
